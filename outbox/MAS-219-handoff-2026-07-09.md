# MAS-219 (MAS-84) — Handoff from Jericho (consolidated-gateway wake)
## 2026-07-09 17:05 DXB

Paperclip wake for DEV (agent 44c1e448) routed to Jericho — DEV's dedicated gateway doesn't exist post-consolidation. Prior run failed HTTP 429 (rate limit on Paperclip update, not on build).

## Disposition: BLOCKED (needs human decisions)

Preflight verification is COMPLETE. Full report at /opt/data/jericho/reports/MAS-84-preflight-2026-07-09.md

### Key finding: MAS-84 is stale

The promote-to-prod.yml / rollback-prod.yml workflows referenced in the ticket are RETIRED. Production now runs on Railway (MAS-239). The actual promotion path is:
- provision-railway.yml (full idempotent reconcile) or deploy-railway.yml (incremental)
- db-deploy.yml (dispatch-only: target=prod, confirmation=deploy-prod)
- Both gated by RAILWAY_DEPLOY_ENABLED=true

### What's verified green (from repo)
- 37 migrations, newest 20 days old — well over 48h requirement
- PostGrid webhook route exists (Lob retired per MAS-16)
- Resend webhook route exists
- Railway workflows verified end-to-end (2026-06-24)
- CI workflow (ci.yml) exists with lint + typecheck + test + e2e gates

### What needs human verification (can't check from VPS)
- RAILWAY_DEPLOY_ENABLED repo var = true
- Stripe live products in stripe_product table
- Stripe live webhook secret in Railway env
- Resend domain auth on memories.express
- Yana's super_admin profile
- OpenClaw VPS pointing at prod Supabase (MAS-78: no commits found)

### Railway hardening checklist (9 items)
See infra/supabase/railway/README.md §221-233. All 9 must be addressed before prod cutover.

### Rollback plan prepared
- App: railway rollback --service app or redeploy previous commit
- DB: forward-only migration (never revert)
- Emergency: disable RAILWAY_DEPLOY_ENABLED, repoint DNS

## Paperclip action needed
Jericho cannot mutate MAS-219 (auth boundary: agent 20cb56be != DEV 44c1e448). Carlos or admin must:
1. Update Linear MAS-84 with the Railway promotion path
2. Close MAS-219 as "done" or "blocked" in Paperclip
3. Or: recover DEV's gateway so it can self-update

## Recommendation
Close MAS-219 with resolution: superseded. The preflight verification is complete. Michael drives the actual promotion timing + Yana coordination. The issue description needs updating to reflect Railway path (or file a new ticket specifically for Railway prod promotion).
