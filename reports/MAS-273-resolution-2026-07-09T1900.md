# MAS-273 Resolution Report
## 2026-07-09 ~19:00 UTC — source_scoped_recovery_action

### Wake Reason
Paperclip `source_scoped_recovery_action` — prior runs (34d06b32, 8f688d85) confirmed completion but issue never reached terminal state.

### Audit Result: COMPLETE — no rebuild needed

All deliverables verified on `memories-express-mvp-cp` (canonical repo):

| Deliverable | Path | Status |
|---|---|---|
| Handler | `lib/services/jobs/cleanupOrphanUploads.ts` | 118 lines, safety-gated behind `CLEANUP_ORPHANS_ENABLED` |
| Cron route | `app/api/cron/cleanup-orphan-uploads/route.ts` | 71 lines, CRON_SECRET auth, dedup check |
| Worker schedule | `workers/cron/src/index.ts` line 26 | `"0 3 * * *"` mapped to route |
| find_orphan_uploads RPC | `supabase/migrations/20260602000100_find_orphan_uploads_rpc.sql` | 63 lines, 4 reference checks, oldest-first ordering |
| GIN index | `supabase/migrations/20260512000200_init_tables.sql` line 300 | `CREATE INDEX ... ON user_template USING gin (data)` |
| Enum entry | `supabase/migrations/20260512000100_init_enums.sql` line 53 | `cleanup_orphan_uploads` in `scheduled_job_kind` |
| pgTAP test | `supabase/tests/find_orphan_uploads.sql` | Present |
| vitest (handler) | `tests/lib/services/jobs/cleanupOrphanUploads.test.ts` | 159 lines |
| vitest (route) | `tests/api/cron-cleanup-orphan-uploads.test.ts` | Present |

### Safety Guard
Handler is deliberately disabled via `CLEANUP_ORPHANS_ENABLED !== "true"` guard at line 42. The comment explains: legacy rows with full signed URLs in `user_template.data` won't match bare-path comparison in `find_orphan_uploads`, risking permanent deletion of referenced assets. Guard stays until legacy rows are migrated or RPC normalizes both sides.

### Disposition
Done — work complete, safety-gated. Move Paperclip issue to `done`.
