# MAS-164 / MAS-212 Resolution Report
**Date**: 2026-07-09
**Agent**: Jericho (consolidated-gateway handler)
**Disposition**: DONE — code complete and merged; no rebuild needed

## Wake Analysis

Paperclip wake `dffd5776` failed with `hermes_gateway_connect_failed: ECONNREFUSED 172.19.0.1:8642`. This is the consolidated-gateway pattern: DEV's dedicated gateway was intentionally decommissioned post-Jul-2026 fleet consolidation. The prior run completed the work but couldn't report back.

## Audit Results

### MAS-212 (maxDuration stopgap) — MERGED
- Commit: `f5d7270` — "fix: add maxDuration=300 to generate route" (PR #108)
- Commit: `d870e49` — same, pre-PR squash
- Branch: `origin/masterblox/mas-212-generate-maxduration` (stale, merged)

### MAS-218 (async generation) — MERGED  
- Commit: `212b76f` — "Async AI generation (#190)" — full rearchitecture
- Commit: `9a463b6` — "Async AI generation: kickoff + worker + polling + quota + single-flight + expire cron"
- Commit: `db16654` — "Bound provider image calls with a timeout + generation instrumentation"
- Branch: `origin/dev/mas-218-async-generation` (stale, merged)

### Current State (origin/main, HEAD == 638d3d7)

**route.ts** (`app/api/ai/generate/route.ts`):
- `maxDuration = 300` — set
- Route returns 202 immediately, model call runs in `after()` task
- Comment explicitly documents MAS-218 async architecture

**_shared.ts** (`app/api/ai/_shared.ts`):
- Uses `after(() => { runGenerationWorker(...) })` at line 165
- Returns `{ status: 202 }` at line 180
- Full async kickoff: insert `requested` row → return 202 → worker in after()

**async.ts** (`lib/services/ai/async.ts`): 9018 bytes, present
- kickoffGeneration, runGenerationWorker, getGenerationJobStatus
- Single-flight deduplication
- Quota checks
- Expiry cron integration

**http.ts** (`lib/services/ai/http.ts`): 2056 bytes, present
- fetchWithTimeout() with AbortController
- Default 120s timeout (under route maxDuration 300, under client poll 180s)
- Overridable via AI_PROVIDER_TIMEOUT_MS

## Acceptance Criteria Check

| Criterion | Status | Evidence |
|-----------|--------|----------|
| maxDuration set on generate route | DONE | `route.ts:10`: `export const maxDuration = 300` |
| Generation off request path (async) | DONE | `_shared.ts:165`: `after(() => runGenerationWorker(...))` |
| Route returns fast (202) | DONE | `_shared.ts:180`: `{ status: 202 }` |
| Provider timeout protection | DONE | `http.ts`: fetchWithTimeout(120s) |
| Worker instrumentation | DONE | async.ts: kickoff_ms, worker_start, provider_ms, upload_ms, total_ms |

## Remaining

Only the paid provider path (MAS-17, `gpt-image-1` via OpenAI) remains unshipped. That's a separate ticket still in Backlog and not blocking MAS-164/MAS-212 closure.

## Stale Branches

Three merged branches still on origin — cleanup candidate:
- `origin/dev/mas-212-finish-ai-generation-timeout`
- `origin/dev/mas-218-async-generation`
- `origin/masterblox/mas-212-generate-maxduration`
