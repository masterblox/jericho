# MAS-189 Sweep — 2026-07-07

## Issue
MAS-189 — Fix customer_provision_failed on /account/billing — trim env secrets at boundary
Source: Linear MAS-178, Paperclip cf0f12cc

## Wake Payload
- Reason: transient_failure_retry (last run a6c374db failed: hermes_gateway_rate_limited HTTP 429)
- Status: in_progress, priority: high
- Consolidated agent: DEV (44c1e448) — no dedicated gateway, routed through Jericho

## Audit Result: ALL CODE WORK COMPLETE ON MAIN

Every deliverable from the issue's scope is already merged into `main`. The run that hit the 429 apparently completed all work before the rate-limit killed the reporting path.

### Verification

| Deliverable | Status | Evidence |
|---|---|---|
| lib/env.ts — .trim() on all string secrets | DONE | 20 `.trim()` calls on main, confirmed via `git show main:lib/env.ts` |
| tests/api/billing/checkout.test.ts (6+ tests) | DONE | 262 lines, 8 test cases on main |
| tests/lib/env.test.ts (trim pinning) | DONE | 122 lines, 12 test cases on main |
| .env.example — trailing whitespace header | DONE | "Whitespace gotcha" section present on main |
| 04-agent-conventions.md — Env values section | DONE | "Env values must not carry trailing whitespace" section at line 395 on main |

### Git state
- Branch: dev/mas-85-smoke-spec (stale working branch from prior ticket)
- All MAS-189 files: no diff from main (changes arrived through prior commits)
- No uncommitted changes, no stashes

### Test execution
Blocked: pnpm/node_modules not installed. Corepack fails with EACCES on /usr/local/bin/pnpm symlink. Tests could not be verified at runtime, but they exist on main and match the issue's scope exactly.

## Remaining: Manual Vercel Cleanup (Item 5)

Per the issue:
```
vercel env rm STRIPE_SECRET_KEY preview && vercel env add STRIPE_SECRET_KEY preview
# repeat for STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, NEXT_PUBLIC_APP_URL
# also check production: vercel env pull .env.production --environment=production
```

Code-side `.trim()` makes this non-urgent — next deploy unbreaks staging on its own. But Vercel-side cleanup should still happen for clean .env.preview reads.

## Paperclip Status

Cannot mutate DEV's Paperclip issue (403 authorization boundary — Jericho is agent 20cb56be, DEV is 44c1e448). Issue remains `in_progress` in Paperclip until DEV gateway recovers or admin closes.

## Recommendation

1. Close MAS-189 — code work is complete and merged
2. Manual Vercel cleanup per item 5 (Carlos or operator)
3. Mark Linear MAS-178 as Done
