# MAS-247 Sweep Report — 2026-07-07

## Result: COMPLETE (code on disk, untracked)

Previous run `f60184b0-b1ff-442a-8af8-971589b2f327` failed with transient 429 rate limit AFTER completing all implementation. This was a self-documenting completion pattern.

## Deliverables Verified

### templates.rename procedure
- `lib/trpc/routers/templates.ts:573-606`
- `workgroupProcedure("editor")` — scoped to editor+ role
- `z.string().trim().max(200)` — trim before length cap
- Empty/whitespace collapses to "Untitled design"
- Cross-workgroup IDs return NOT_FOUND (0-row update)
- No OCC gating (name column is independent of canvas version)

### CanvasChrome inline title
- `app/(authed)/account/designs/[id]/_components/CanvasChrome.tsx:54-186`
- Inline-editable: click title button → input appears
- Enter commits, Escape reverts, blur commits
- Debounced via tRPC mutateAsync (no timer — immediate on commit)
- Loading spinner during mutation
- Test IDs: `canvas-title`, `canvas-title-button`, `canvas-title-input`

### Designs list
- `app/(authed)/account/designs/page.tsx:111`
- Already shows `d.name?.trim() ? d.name : "Untitled design"`
- No change needed — the list already reads from `templates.list` which includes `name`

### Name prop chain
- `[id]/page.tsx:81`: passes `name={template.name}` to EditorViewportGate
- `DesignEditor.tsx:338`: passes `name={name}` to CanvasChrome

### Vitest (7 test cases)
- `tests/lib/trpc/routers/templates.test.ts:1155-1304`
- Trim whitespace, whitespace-only → "Untitled design", empty → "Untitled design"
- Scope invariant (id + workgroup_id + deleted_at IS NULL)
- NOT_FOUND for cross-workgroup/deleted/unknown
- INTERNAL_SERVER_ERROR on DB error
- BAD_REQUEST for >200 chars (zod gate)
- FORBIDDEN for member role
- FORBIDDEN for non-members

### CanvasChrome test mock
- `tests/app/designs/CanvasChrome.test.tsx:35` — rename mock present

### Playwright e2e (NEW — written this run)
- `tests/e2e/designs-rename.spec.ts`
- Test 1: rename via chrome → navigate to designs list → assert title visible
- Test 2: whitespace-only name → falls back to "Untitled design"
- Test 3: Escape reverts to last confirmed name

## Gap: CI Gates

Cannot run `pnpm typecheck && pnpm lint && pnpm test` in this container — all pnpm-exec Node processes timeout due to resource constraints. The code needs a PR → CI run for final validation.

## Repo State

- Repo: `/opt/data/memories-express` (Mechanica-Labs/memories-express)
- Branch: `carlos/mas-188-support-inbox-contact-form`
- All files untracked (`??`) — known sparse-tracked state
- No git commit for MAS-247

## Recommended Action

1. Commit the files (MAS-247 scope only): templates.ts, CanvasChrome.tsx, DesignEditor.tsx, [id]/page.tsx, templates.test.ts, CanvasChrome.test.tsx, designs-rename.spec.ts
2. Open PR with `[MAS-247]` prefix
3. Let CI run typecheck + lint + test
4. Close MAS-14 in Paperclip once CI passes (or admin-close if agent can't reach Paperclip)
