# MAS-161 / MAS-189 Resolution Report
**Date:** 2026-07-09 ~16:30 DXB
**Resolved by:** Jericho (on behalf of DEV — DEV gateway 429'd)
**Disposition:** DONE — code already in main

## What happened

Paperclip wake for MAS-161 (MAS-189 — /account/settings) came as transient_failure_retry. Previous run 26496f38 failed with `hermes_gateway_rate_limited` (HTTP 429). The gateway rate limit was handled by the consolidated-gateway wake routing — Jericho picked up the task.

## Audit finding

The work was ALREADY COMPLETE. Original commit d88f60e (lofimichael/send-flow, May 31) was merged into main via PR #90 (8f4a81d "Send flow end-to-end"). Migration renumbered from 20260527 to 20260601 during merge.

## What's in main

### UI (7 files)
- `app/(authed)/account/settings/page.tsx` — settings shell with 4 sections
- `app/(authed)/account/settings/_components/ProfileSection.tsx` — display_name + image_url edit, avatar upload via signed-URL
- `app/(authed)/account/settings/_components/EmailSection.tsx` — Supabase auth.updateUser email change with verification
- `app/(authed)/account/settings/_components/PasswordSection.tsx` — re-auth + password change (min 8 chars)
- `app/(authed)/account/settings/_components/DeleteAccountSection.tsx` — type-"DELETE" confirm modal
- `app/(public)/goodbye/page.tsx` — post-delete landing page
- `components/ui/UserMenu.tsx` — Settings link added

### API (2 files)
- `lib/trpc/routers/profile.ts` — 4 procedures: get, updateProfile, updateEmail, deleteSelf
  - deleteSelf guards: sole-owner CONFLICT check before RPC, RPC → best-effort logAdminAction
- `lib/trpc/router.ts` — profile router mounted as `profile: profileRouter`
- `lib/db/rpc.ts` — deleteProfileSelf typed wrapper (L93-99)

### Migrations (2 files)
- `supabase/migrations/20260601000300_add_delete_profile_self_rpc.sql` (80 lines) — SECURITY DEFINER RPC: soft-delete profile, anonymize PII, revoke tokens, suspend workgroups, preserve ledger
- `supabase/migrations/20260601000400_lock_profile_status_columns.sql` (58 lines) — tg_prevent_profile_status_escalation trigger (locks deleted_at/banned* from direct user writes)

### Tests (3 files)
- `supabase/tests/delete_profile_self.sql` (137 lines) — pgTAP: soft-deletes, PII anonymized, tokens revoked, ledger untouched, idempotent
- `tests/lib/trpc/routers/profile.test.ts` (274 lines) — vitest: updateProfile SET-keys regression guard, deleteSelf sole-owner CONFLICT, happy-path, idempotent, UNAUTHORIZED
- `tests/e2e/account-settings.spec.ts` — Playwright: edit display_name, password change, email change, delete account → goodbye

### Infrastructure
- `lib/services/admin/auditLog.ts` — logAdminAction helper (exists, used by deleteSelf)
- `lib/db/types.gen.ts` — delete_profile_self type present
- Auth router has `updatePassword` procedure

## Paperclip status

Attempted to update MAS-161 to `done` — received 403 "Issue is outside this actor's authorization boundary" (Jericho agent ID != DEV agent ID). DEV's gateway is rate-limited/down per fleet consolidation. Manual closure needed when Paperclip/DEV recovers.
