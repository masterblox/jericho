# MAS-334 Wake Resolution — 2026-07-10 16:00 DXB

## Issue
MAS-334 — Budget persistence: budget table + /api/budgets on the plan pattern

## Wake Type
`issue_continuation_needed` — prior run 4d25849a completed successfully. This is a liveness continuation wake.

## Audit Result: WORK COMPLETE — NO REBUILD

All 7 work items verified on `dev/mas-343-bypass-budget-screen` (commit 866b5c2a) in architect-ai repo:

### 1. Migration — PASS
File: `supabase/migrations/20260710000000_budgets.sql`
- budget table with uuid PK, payload jsonb, engine text, user_id uuid nullable, created_at timestamptz
- RLS enabled (`alter table public.budget enable row level security`)
- `grant select, insert on table public.budget to service_role`
- No policies — intentional per spec

### 2. POST /api/budgets — PASS
File: `src/app/api/budgets/route.ts`
- Imports: isServerPersistenceConfigured, env as siteEnv, createServiceClient, ratelimit/getClientIp, Json
- Rate limited (20/min per IP)
- 503 when persistence unconfigured
- Engine constant `engine-b` from module constant, not user input
- Returns `{ id, url: "<site>/budget?id=<uuid>" }`

### 3. GET /api/budgets/[id] — PASS
File: `src/app/api/budgets/[id]/route.ts`
- UUID regex guard → 404 on mismatch
- 503 when persistence unconfigured
- Service-role read of `payload, engine, created_at`
- `Cache-Control: public, max-age=300`
- Returns stored payload directly (no recompute)

### 4. BudgetPageClient hydration — PASS
File: `src/app/budget/BudgetPageClient.tsx`
- Reads `?id` via `useSearchParams()`
- When shareId present, fetches GET /api/budgets/[id]
- Renders stored payload verbatim — no `computeEstimate` re-run
- Q&A dialog hidden when hydrated from share

### 5. BudgetPageClient Share action — PASS
File: `src/app/budget/BudgetPageClient.tsx`
- `handleShare` POSTs current Estimate to /api/budgets
- On success: `window.history.replaceState(null, "", '/budget?id=<uuid>')`
- State machine: idle → saving → saved/error
- Uses Share2 + Check icons from lucide-react

### 6. pgTAP — PASS
File: `supabase/tests/database/schema_test.sql`
- `select plan(17)` — bumped from 14
- `has_table('public', 'budget', ...)` 
- `has_column('public', 'budget', 'payload', ...)`
- RLS check via `relrowsecurity` on `public.budget`

### 7. Working tree — PASS
- `.git/HEAD` → `ref: refs/heads/dev/mas-343-bypass-budget-screen`
- Branch HEAD → `866b5c2a` (matches log entry "[MAS-334] Budget persistence")

## Blockers
- GitHub OAuth expired — branch cannot be pushed to origin
- Paperclip unreachable — deferred close queued

## Disposition: in_review
Work complete. Branch ready for PR once OAuth is restored.
