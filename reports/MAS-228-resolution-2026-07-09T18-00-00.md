# MAS-228 / MAS-37 Resolution Report

Date: 2026-07-09 ~18:00 DXB
Resolution: COMPLETE — no rebuild needed

## Wake Context

- Wake reason: `transient_failure_retry`
- Prior run: `80aadf46` — failed with `hermes_gateway_connect_failed: ECONNREFUSED 172.19.0.1:8642`
- Root cause: consolidated gateway — DEV's dedicated gateway doesn't exist as a service post-Jul 2026 fleet consolidation. The ECONNREFUSED is a routing error, not a code failure.
- Continuation summary: zero files/routes touched, zero adapter result captured — the gateway never ran the agent.

## Audit Findings

### Git History
- Original implementation: `85afcc8` — `[MAS-37] uploads.sign + uploads.confirm tRPC procedures`
- Merged via: `cf75e83` — `[MAS-24][MAS-37] Storage RLS hardening + signed-upload endpoint (#34)`
- `cf75e83` IS an ancestor of `origin/main` — merged to main

### Implementation (all on main)

| Component | File | Status |
|-----------|------|--------|
| tRPC router (sign, confirm, list, remove, signedUrl, resignPaths) | `lib/trpc/routers/uploads.ts` (457 lines) | COMPLETE |
| Upload constants (quota, mime whitelist, limits) | `lib/uploads/constants.ts` | COMPLETE |
| Signed URL service (signUploadUrl, signUploadPaths, signDesignForDisplay) | `lib/services/storage/signedUrl.ts` | COMPLETE |
| Vitest: sign/confirm flow | `tests/lib/trpc/routers/uploads.test.ts` | COMPLETE |
| Vitest: signedUrl/resignPaths canvas bridge | `tests/api/uploads-signed-url.test.ts` | COMPLETE |
| pgTAP: quota SUM invariant | `supabase/tests/upload_quota.sql` | COMPLETE |

### Acceptance Criteria vs Implementation

| AC | Implementation |
|----|---------------|
| POST /api/uploads/sign — signed upload URL | tRPC `uploads.sign` procedure (CLAUDE.md convention: tRPC, not REST) |
| POST /api/uploads/confirm — insert upload row | tRPC `uploads.confirm` procedure |
| Membership gating (workgroup_id from body) | `workgroupProcedure("member")` — server-side is_workgroup_member check |
| Mime whitelist (jpg/png/webp/heic) | `UPLOAD_MIME_TO_EXT` in constants.ts — jpg/png/webp/heic/heif |
| Size cap | `MAX_UPLOAD_BYTES = 25MB` (ticket said 50MB; code is source of truth per CLAUDE.md) |
| Quota: 5GB per workgroup | `UPLOAD_QUOTA_BYTES_PER_WORKGROUP = 5 * 1024^3` |
| Idempotent confirm | Duplicate path returns existing row with `idempotent: true` |
| Error codes: 403, 400, 413, 502 | TRPCError codes: FORBIDDEN, BAD_REQUEST, PAYLOAD_TOO_LARGE, BAD_GATEWAY |

### Convention Drift (ticket vs code)

The ticket prescribes `POST /api/uploads/sign` and `POST /api/uploads/confirm` as Next.js App Router route handlers. Per CLAUDE.md API surface convention, all app data access goes through tRPC. The signed-URL upload flow is listed as an exception that's "bracketed by tRPC procedures" — exactly how this was implemented. The tRPC router provides the same functionality with better type safety and the existing workgroupProcedure auth pattern.

MAX_UPLOAD_BYTES is 25MB in code vs 50MB in ticket. The ticket was written pre-implementation; the actual storage bucket migration (`20260519120000_tighten_storage_bucket_limits.sql`) sets the limit, and constants.ts mirrors it. Per CLAUDE.md: "Code is the source of truth."

## Verdict

NO REBUILD NEEDED. The feature is fully implemented, tested (vitest + pgTAP), and merged to main via PR #34. The ECONNREFUSED wake was a fleet routing error, not a code failure. Zero files changed, zero commits needed.

## Action

Queue deferred close for Paperclip issue MAS-228. Paperclip API is health-only for Jericho's key — close must go through the paperclip-recovery-closer cron (every 30m, no_agent).
