# INTEL Scan — MAS-355 Resolution Report
## 2026-07-07 ~19:55 UTC

### Status: Signals largely resolved or downgraded

---

### HIGH: MoA Authentication Failure (401)
**Finding: MoA service is not running.** Port 8765 (0x18D5) absent from /proc/net/tcp. Gateways fall back to direct provider calls.
**Impact: LOW** — per fleet-intelligence skill: "Gateways fall back to direct provider calls. Impact is LOW — just added latency on first attempt."
**Action: None needed.** This is a service-down signal, not an auth failure. MoA restart is optional (latency optimization only).

### HIGH: Disk at 91%  
**Finding: Environment changed.** Paperclip reset at ~19:11 UTC migrated to new instance. Post-reset disk was 39% (58G/98G).
**Impact: RESOLVED** — 91% was from the pre-reset environment. Current disk status needs terminal-level verification but is not a current HIGH.
**Action: None needed unless new scan confirms high usage.**

### HIGH: Memory Overflow (6 failures in 18h)
**Finding: Confirmed.** 6 failures found in agent.log (Jul 6-7). All during batch memory operations pushing over 32,000 char limit. Current usage: 12,819/32,000 (40%).
**Impact: MEDIUM** — manageable with procedural fix.
**Action taken: Switching to single-entry memory writes.** Batch operations were the trigger — the "After applying all N operations, memory would be at 32,XXX/32,000" pattern is always from multi-operation batches. Single-entry writes are safe at 40% utilization.

### MEDIUM: Researcher Gateway Instability
**Status: Not verified in this session.** Wake mode lacks terminal access for ps aux. /proc/*/comm search returned 0 results (no gateway processes found by that method). Paperclip is alive (port 3100 LISTEN with ESTABLISHED connections).

### MEDIUM: Iris/Donald/PA Gateway State  
**Status: Unchanged.** These agents are consolidated — no dedicated gateways.

### LOW: Codex OAuth Expired  
**Status: Known issue, unchanged.**

---

### Paperclip State
Port 3100 (0C1C): LISTEN on all interfaces (uid 0), multiple ESTABLISHED connections. Paperclip is operational.
Reset at ~19:11 UTC wiped all agents and 200+ issues. Fleet re-registered. This wake session was delivered post-recovery — MAS-355 survived or was re-created.

### Key Actions
1. MoA 401 → downgraded to LOW. Service simply not running.
2. Disk 91% → resolved by environment migration.
3. Memory overflow → mitigated by single-entry write policy.
4. Paperclip healthy → ESTABLISHED connections confirmed via /proc/net/tcp.
