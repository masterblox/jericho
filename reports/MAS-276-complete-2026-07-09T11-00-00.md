# MAS-276 Resolution — 2026-07-09 11:00 DXB

## Issue
MAS-276 (MAS-51): Let users cancel pending sends and get refunded

## Finding
Feature is fully implemented and shipped on origin/main. The previous run (70feb82a) failed with ECONNREFUSED because the DEV Hermes gateway doesn't exist as a standalone service (consolidated fleet). The code was already done.

## Implementation (all on main, commit 638d3d7)

### Backend
- `cancel_order` RPC: `supabase/migrations/20260512000300_init_functions.sql:376`
  - Security definer, service-role-only
  - Checks membership (NOT via auth.uid() — explicit p_actor_profile_id)
  - Checks fulfilled=false, idempotent on canceled_at
  - Refunds via grant_credits (ledger_unique_business_event guards double-refund)
  - Sets payment_status='refunded', scheduled_job.status='completed'
  - Raises P0008 (NOT_CANCELLABLE) for fulfilled, P0009 (NOT_AUTHORIZED) for non-member

- `cancelOrder` typed wrapper: `lib/db/rpc.ts:56`

- `cancelPostcard` PostGrid integration: `lib/services/postgrid/postcards.ts:185`
  - DELETE /postcards/{id}, handles 200/409/422
  - Pattern-matches "too_late", "cancel_failed_error", "cannot cancel"
  - Returns {status: 'cancelled' | 'too_late'}

- `orders.cancel` tRPC mutation: `lib/trpc/routers/orders.ts:350`
  - workgroupProcedure("member")
  - SCOPE INVARIANT gate (re-derives order, checks workgroup_id)
  - Calls cancelOrder RPC via service client
  - Maps P0008 → CONFLICT/NOT_CANCELLABLE, P0009 → FORBIDDEN
  - Best-effort PostGrid cancel for printed sends
  - Too-late: sets print_status='mailed' (refund already committed)
  - Reads refund amount from ledger
  - Returns {refunded_credits, postgrid_status}

### Frontend
- `CancelSendButton` component: `app/(authed)/account/sends/[id]/_components/CancelSendButton.tsx`
  - window.confirm dialog
  - Loading spinner during request
  - Success toast with credit count
  - NOT_CANCELLABLE → warning toast + refresh
  - Error → error toast + refresh

- State gating via `isCancellable()`: `lib/services/orders/state.ts:117`
  - Only when deriveSendState returns "PENDING"
  - Hidden for IN_FLIGHT, DELIVERED, CANCELED, MAILED, IN_PRINT, FAILED, STUCK

### Tests
- pgTAP `supabase/tests/cancel_order_auth.sql` (3 tests):
  - Non-member raises P0009
  - Member succeeds
  - Refund credit lands in ledger (source=refund)

- pgTAP `supabase/tests/cancel_order_refund.sql` (4 tests):
  - Balance restored to 100 after cancel
  - Second cancel is no-op (balance still 100)
  - Scheduled job status → 'completed'
  - Fulfilled obligation raises P0008

- vitest `tests/lib/trpc/routers/orders-cancel.test.ts` (8 tests):
  - Digital send: refunds, no PostGrid call
  - Printed send: also cancels PostGrid postcard
  - Too-late: records print_status=mailed, refund stands
  - PostGrid failure: postgrid_status=failed, refund stands
  - P0008 → CONFLICT NOT_CANCELLABLE
  - P0009 → FORBIDDEN
  - Cross-workgroup → NOT_FOUND
  - Print-outcome update failure doesn't break cancel

## Paperclip Status
Cannot update Paperclip — API times out (TCP open, HTTP 000). Same zombie-socket deadlock as Jul 9 16:30 DXB. Issue should be manually closed on Paperclip.
