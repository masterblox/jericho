# MAS-66 / MAS-145 — Resolution Report
## 2026-07-09 ~14:30 DXB (UTC+4)

### Wake Summary

Consolidated-gateway 429 pattern. Previous run `320b3362` failed with `hermes_gateway_rate_limited` — zero files/routes touched, gateway rate-limited before DEV agent ran at all. Wake routed to Jericho for direct handling per consolidated-gateway ECONNREFUSED/429 pattern.

### Issue

MAS-145: Decide Vercel branch model — Option A (restore documented `production` branch via burner subdomain) vs Option B (simplify, keep `main` as production branch).

### Audit Findings

The decision was de facto made by MAS-239 (Railway deployment, landed June 2026). Production deploys via Railway, not Vercel branch promotion. Neither Option A nor Option B was chosen — a third path (Railway) superseded both.

Evidence:

1. **Architecture doc** (`01-architecture.md` lines 383-413): Vercel `promote-to-prod.yml`/`rollback-prod.yml` marked RETIRED. Production-branch promotion model labeled "Historical (original Vercel plan)" and "Superseded by the Railway path (MAS-239)."

2. **No promote workflow**: `.github/workflows/promote-to-prod.yml` does not exist in the repo.

3. **README.md** (line 237-248): Documents Railway as deploy target, Vercel promote/rollback workflows explicitly retired.

4. **Agent conventions** (`04-agent-conventions.md` line 322): "Production deploys via Railway (MAS-239). The Vercel production-branch promotion is retired."

5. **Disaster recovery** (`disaster-recovery.md` line 300): "The Vercel prod deploy CI (promote-to-prod.yml / rollback-prod.yml) is retired (MAS-239)."

6. **env.md**: Vercel env vars table documents the Interim state (Vercel as app host today, Railway at cutover). No Vercel production-branch assumption in any env scope.

### Remote `production` Branch

A `production` branch exists on origin (commit `a227ddd`, different from main at `638d3d7`). No local tracking branch. This is an archaeological artifact from the initial Vercel setup when Option A was the plan. No Vercel deployment listens to it, no workflow pushes to it. At cutover (Railway), this branch becomes entirely irrelevant.

Recommendation: delete the remote `production` branch after confirming no Vercel project references it. Low priority — harmless artifact.

### One Minor Stale Reference

`00-summary.md` line 117: "One repo, one branch strategy: main (production) + feature branches." This is actually correct in the Railway model (main IS the production branch), but "production" in parentheses is ambiguous with the retired Vercel `production`-branch concept. Consider changing to "main (deployed to Railway)" for clarity — purely cosmetic.

### Resolution

**MAS-145 is SUPERSEDED by MAS-239.** No code changes needed. No architecture doc changes needed (already updated). Close the Linear ticket as "Canceled" or "Won't do" with reason: "Superseded by MAS-239 (Railway deployment — production deploys via Railway, not Vercel branch promotion)."

### Action Items

1. [x] Close MAS-145 in Linear (status: Canceled, reason: superseded by MAS-239) — DONE 2026-07-09 ~19:30 DXB
2. [x] Close MAS-66 in Paperclip (status: done) — deferred close written to outbox (Paperclip auth deadlocked)
3. [x] Fix docs: 00-summary.md line 117 "main (production)" → "main (deployed to Railway)" + Vercel promotion retired note
4. [x] Fix docs: 04-agent-conventions.md lines 55 and 458 — removed stale `production` branch references
5. [ ] Optional: delete remote `production` branch (harmless artifact at a227ddd — low priority)
