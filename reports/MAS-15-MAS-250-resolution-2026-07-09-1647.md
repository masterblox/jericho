# MAS-15 / MAS-250 Resolution Report

**Date:** 2026-07-09T16:47:16Z
**Disposition:** done — work completed in prior commit

## Verification

Prior run bf224a53 failed with HTTP 429 (Hermes gateway rate limit) before doing any work.
However, the MAS-250 work was already completed in commit e991708 (multi-ticket batch #160)
which is on origin/main.

## Acceptance Criteria — All Met

1. **Delivered fallback:** `deliveryStep()` in `EngagementTimeline.tsx` provides
   "Sent" step from `fulfilled_at` when `delivered_at` is null. No more empty
   "Delivered —" under populated "Opened".

2. **Single timezone:** `formatStamp` in `lib/utils/formatDate.ts` shared by
   `DeliveryDate` (client component) and `EngagementTimeline`. Both format in
   browser-local zone. Kills UTC-vs-local ordering paradox.

3. **Tests:**
   - `tests/app/sends/delivery-display.test.tsx`: deliveryStep fallback + TZ
     consistency render test
   - `tests/lib/utils/formatDate.test.ts`: formatStamp unit tests
   - Commit e991708: 1459 vitest passing, typecheck + lint clean

4. **Stat labels:** "Scheduled for" (PENDING), "Delivery estimate" (IN_FLIGHT),
   "Delivered" (DELIVERED) — uses `fulfilled_at` once dispatched.

## Files

- `app/(authed)/account/sends/[id]/_components/EngagementTimeline.tsx` — deliveryStep()
- `app/(authed)/account/sends/[id]/_components/DeliveryDate.tsx` — client TZ component
- `app/(authed)/account/sends/[id]/page.tsx` — stat labels, fulfilledAt prop
- `lib/utils/formatDate.ts` — shared formatStamp
- `tests/app/sends/delivery-display.test.tsx` — deliveryStep + TZ tests
- `tests/lib/utils/formatDate.test.ts` — formatStamp unit tests

## No Remaining Work

All ACs satisfied. Issue should be marked `done`.
