# MAS-332 Resolution — 2026-07-09 18:12 UTC+4

## Disposition: DONE

## Prior Run (cb98ea04) — FABRICATED AUDIT

The prior run checked /opt/data/memories-express-mvp-cp and claimed:
- regions.test.ts exists on main (197 lines, commit ba7e57f)
- computeEstimate doesn't exist — blocker
- All other deps in place

ALL of this was wrong. The code lives in architect-ai (/opt/data/repos/architect-ai), not memories-express. This is cross-repo false positive #4 (after MAS-170, MAS-237, MAS-275).

## Actual State (architect-ai)

All dependencies were BUILT:
- 01 (vitest) — in place
- 04 (resolveConstructionTax) — BUILT, imported by test
- 06 (planner-trust-fix) — NOT BUILT — vatRate: 0 still in global-calc.ts:57
- 10 (computeEstimate compose) — BUILT, estimate.ts exists (132 lines)

## Fix Applied

global-calc.ts:57 — changed `vatRate: 0` to `vatRate: resolveConstructionTax(region)?.rate ?? 0`
Added import for resolveConstructionTax.

## Test Results

```
✓ 4 test files passed (37 tests)
  ✓ regions.test.ts — 23 tests (all 6 regions)
  ✓ format.test.ts — 5 tests
  ✓ resolveConstructionTax.test.ts — 8 tests
  ✓ smoke.test.ts — 1 test
```

## Acceptance Criteria

- [x] npm run test:unit runs green across all regions (6 regions, 23 tests)
- [x] Iterates listRegions() (no hardcoded names)
- [x] VAT assertion uses toBeCloseTo with null-guard
- [x] Tier assertion uses 3 explicit quality inputs
- [x] Guard comment documents that reverting vatRate to 0 makes test fail

## Commit

1f3b1eb on origin/main — [MAS-332] Fix vatRate=0 regression — wire to resolveConstructionTax, green per-region test suite

## Paperclip

Stage 4/5 (HTTP 000). Deferred close written:
/opt/data/jericho/outbox/paperclip-deferred-close-MAS-332.json

## Cross-Repo False Positives (updated)

1. MAS-170 — commit 8ca3681 on memories-express
2. MAS-237 — commit 3b85d40 on memories-express
3. MAS-275 — commit 244b9dd on memories-express
4. MAS-332 — ALL budget code in architect-ai, prior run checked wrong repo

Pattern: When Paperclip wake mentions app/(authed)/ paths, it's memories-express. When it mentions src/lib/budget/, it's architect-ai. Paperclip's issue-to-repo mapping is unreliable.
