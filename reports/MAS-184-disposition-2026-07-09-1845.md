# MAS-184 Disposition — 2026-07-09 18:45 DXB

## Status: DONE (blocked on Paperclip API key upgrade)

## What happened
Transient-failure-retry wake. Prior run (3e5a94be) completed all work, merged to main, then crashed on 429 during Paperclip update. Run d0451205 verified and queued deferred close. Run a0a7bf41 (this one) verified again and fixed the recovery closer.

## Verification (this run)
- HEAD: b371faf (61dd2a2 is ancestor)
- 16 `.trim()` calls in lib/env.ts
- 6 checkout tests + 5 env tests
- Docs in .env.example + 04-agent-conventions.md

## Root cause of wake loop
The `paperclip-recovery-closer` had two bugs:
1. Health check used `/api/health` WITH auth header — but the auth middleware hangs on this health-only key, causing a 15s timeout. The closer silently skipped all deferred closes thinking the API was down.
2. close_issue() used company-prefixed path `/api/companies/{id}/issues/{id}` — but Paperclip 0.3.1 only has non-prefixed routes `/api/issues/{id}`.

Both fixed. Closer now correctly identifies the auth-degraded state: "HTTP 401 (key health-only — needs agent/issue scope upgrade)".

## Paperclip API state
- Health: 200 (API alive)
- Auth: key `jer_924b41...` is HEALTH-ONLY — all issue-scoped endpoints return 401
- Bootstrap: closed (bootstrapInviteActive: false)
- Deferred closes queued: MAS-184, MAS-273, MAS-507

## Action taken
- Fixed paperclip-recovery-close.py (health check + URL routing)
- Closer cron (8f3e069d48d7, every 30m) now working correctly — will close all 3 issues the moment a properly-scoped API key is provisioned
- No rebuild needed — work is on main

## Blocker
Paperclip API key needs agent/issue scope upgrade. Current key is health-only. Host operator needs to provision a new key or upgrade the existing one.
