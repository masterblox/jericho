# MAS-22 Audit — Supabase Migration Propagation
**Date:** 2026-07-07  
**Agent:** Jericho (via DEV consolidated gateway)  
**Status:** BLOCKED — migrations never pushed to main

## Audit Summary

MAS-21 migrations are complete on disk (35 SQL files, 30 tables, 4 views, 14 functions). The CI pipeline (`db-deploy.yml`) is ready with auto-deploy-to-staging on push to main. But **zero commits containing migration files exist on `origin/main`**. The repo is in sparse-tracked state — all files show as untracked (`??`).

## What Exists (Complete)

| Artifact | Status | Details |
|---|---|---|
| Migration files | 35 `.sql` files | `supabase/migrations/20260512000000_init_extensions.sql` through `20260616000000_revoke_credit_grant_redeem.sql` |
| types.gen.ts | 65KB, 48 entries | 30 tables, 4 views, 14 functions. Comprehensive. |
| db-deploy.yml | Ready | Auto-deploys to staging on push to main when migrations change. Has deploy-staging job with supabase link + db push + seeders. |
| db-ci.yml | Ready | Migration apply + idempotency check + pgTAP. Type drift intentionally NOT gated (per workflow comment). |
| gen-types.yml | Ready | Manual dispatch to regenerate types.gen.ts from local Supabase stack. |
| supabase-config-deploy.yml | Present | Additional deploy workflow. |

## What's Missing (Blocker)

| Gap | Detail |
|---|---|
| Git commit/push | 35 migration files + types.gen.ts + 4 CI workflow files are all **untracked**. `origin/main` has only 2 commits — neither contains migration files. |
| CI runs | Zero db-deploy workflow runs on main. The workflow file hasn't been pushed to GitHub, so it can't fire. |
| Supabase CLI | Not installed on this VPS. Local dev steps (`supabase link`, `supabase db push`) require per-developer machines with creds. |
| Project refs / tokens | No `.env` file, no `supabase/.temp/` (not linked), no SUPABASE_ACCESS_TOKEN on VPS. |

## What Needs to Happen

1. **Push migration files to main** — commit the full `supabase/migrations/` directory + `lib/db/types.gen.ts` + CI workflows to a branch, open PR, merge to main
2. **db-deploy CI auto-fires** — the merge to main with migration changes triggers `deploy-staging` job automatically
3. **Verify CI run** — check staging Supabase Studio for all tables
4. **Per-developer setup** — each dev runs `supabase link --project-ref <ref>` + `supabase db push` on their machine (requires SUPABASE_ACCESS_TOKEN)
5. **Developer project refs** — need to identify each developer's personal Supabase project ref (not available on this VPS)

## GitHub Actions State

- db-deploy on main: **0 runs** (workflow file not on remote)
- db-ci on main: **0 runs** 
- gen-types: **0 runs**
- Rate limit: 4998 remaining (healthy)

## Repo State

- Path: `/opt/data/memories-express`
- GitHub: `Mechanica-Labs/memories-express`
- Current branch: `dev/mas-58-resend-webhook`
- Git status: sparse-tracked (all files `??`)
- origin/main commits: `8eda13a`, `3a6b4fd` (design + MAS-177 only)
